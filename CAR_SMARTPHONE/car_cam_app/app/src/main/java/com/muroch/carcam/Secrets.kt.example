package com.muroch.carcam

// Template. Copy this file to Secrets.kt (same folder) and paste the real
// esp_boards MQTT password from BOILER/dashboard/.env (ESP_BOARDS_MQTT_PASS).
// Secrets.kt is gitignored so the credential never lands in the repo.
object Secrets {
    const val MQTT_PASS = "REPLACE_WITH_ESP_BOARDS_MQTT_PASS"
}
