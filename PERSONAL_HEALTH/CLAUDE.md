# Personal Health

A **Personal Health Record** — a per-person body-metrics tracker. **Not a standalone agent**: it lives as the **Personal Health tab on the Medical page** (`medical.html`), reusing the Medical sidebar entry. Dashboard-only; no LXC service, no `agents`-table row.

## Status (2026-06-25)
**Shipped:** per-person **profile + weight log → BMI**, and a per-person **medications list** with a safety **ℹ️ Info window**.

**People come from the canonical `household_users` table** — the person dropdown is populated from `GET /api/household-users` (the Privacy → Settings → Users list, a real table since 2026-06-25 — REPLACED the old `dashboard_settings.privacy.users` JSON blob, which was deleted). No people are created here; each household member gets a body profile + logs. `ph_profiles.user_id` FKs to `household_users(id)` (resolution still also works by name during transition). If the list is empty the tab points the user to Privacy → Settings → Users.

**Profile** (slow-changing, per person): name, sex, date-of-birth, height, **allergies**, **conditions** (the last two feed the future cross-check). **Weight log** (time-series). **BMI / age / BMI-category / ideal-weight** are computed in the front-end (never stored — can't go stale when height/DOB are corrected).

**Medications** (per person): each pill **row** shows `name · dose · schedule` + Info/Edit/Stop/Del — the **basic info (name/dose/schedule/notes) is edited on the row**, exactly like a normal list. The **ℹ️ Info button** opens a window holding the **extra/safety info** (purpose, ingredients, drug_class, avoid_with, contraindications, side_effects, warnings, prescriber, started) with its **own edit** (`✎ Edit info`). The **schedule model** supports `daily` / `weekly` / `every_n_months` (e.g. 6 = half-year) / `every_n_days` / `once` / `as_needed`, with `times` (HH:MM, comma-sep), `dow`, `interval_n`, `next_due` — built so a **future reminder watcher** can compute each pill's next-due time. **Schedule cell rendering (2026-06-26):** `medSchedText(m, html)` formats the row's schedule; the **`next_due` date renders as `15 Nov 2026`** (via `_phFmtDate`, `toLocaleDateString` `en-GB` day/short-month/year — not ISO) and, in the table (`html=true`), as a **bigger green span** (`#1a7f37`, 0.92rem, bold, 8 px left-gap); the **ℹ️ Info** window uses the same function with `html` falsy → plain formatted text. Dynamic parts (`times`, `dow`) are HTML-escaped in `html` mode since the table cell isn't outer-`esc()`'d.

**Future med-safety cross-check (designed, schema is ready):** flag when a med's **`avoid_with`** / **`contraindications`** / **`ingredients`** conflict with the person's **`allergies` + `conditions`** or their **other active meds**. All the data is captured now; the flag itself is a later step. The UI is worded as a **record / reminder aid — not medical advice**.

**Deferred:** waist + waist-to-height, activity → BMR/TDEE, body-fat %, vitals (HR/BP), **progress charts** (Chart.js already loaded on the page), **goals & achievements**, and the **reminder watcher**. (**Daily steps shipped 2026-06-25** — manual + walking-trip import; see "Daily steps" below.) (`manufacturer` / `mechanism` were considered for meds and dropped — reference-only, not needed for the cross-check.)

## File Locations

| Artifact | Path |
|----------|------|
| Dashboard tab (UI) | `BOILER/dashboard/public/medical.html` → `#tab-personal-health` (tab button calls `phInit()`) |
| Front-end logic | `BOILER/dashboard/public/js/personal-health.js` (`?v=N`) |
| Backend endpoints | `BOILER/dashboard/routes-personal-health.js` (own module; wired into `server.js` via one `require()` line — past the architecture-guard hook) |
| Shared exercise-log helper | `BOILER/dashboard/lib-exercise-log.js` — `logExerciseRoutine(db, profileId)`, required by both routes-personal-health (Done) + routes-reminders (Clear) |
| DB setup migration | `PERSONAL_HEALTH/migrations/setup.sql` |
| Docs | this file + a note in `MEDICAL/CLAUDE.md` |

## Tables (LXC 102, `home_data`)
- **`ph_profiles`** — one row per person: `id, name, sex ('male'|'female'), date_of_birth, height_cm, allergies, conditions, weight_sched, bp_sched, created_at, user_id`. **`user_id` FK → `household_users(id)` ON DELETE CASCADE** (the canonical member identity; backfilled by name 2026-06-25, `ph_profiles.name` kept for transition). **`weight_sched` / `bp_sched`** (JSONB, added 2026-06-26, migration `012_measure_schedule.sql`) — per-person **measure reminder schedule** for the Weight + BP cards: `{freq, interval_n}` or NULL=Off. `freq ∈ daily | weekly | every_n_days | every_n_months` (same options as meds **minus** weekday / day-of-month / time, per user); `interval_n` only for the `every_n_*` freqs. Capture-only today; a **future LXC-104 reminder watcher** will read these + the person's last weight/BP `measured_at` and flag "overdue" when nothing's logged within the window (no `next_due` stored — derived from the last reading). Retention: forever.
- **`ph_measurements`** — weight log: `id, profile_id (FK → ph_profiles ON DELETE CASCADE), measured_at (timestamptz, stamped server-side on Save — same style as BP, no manual date), weight_kg, created_at`. Indexed `(profile_id, measured_at DESC)`. Retention: forever (`auto_clean=false`).
- **`ph_medications`** — pills: `id, profile_id (FK ON DELETE CASCADE), name, dose, freq, interval_n, times, dow, next_due, notes, purpose, ingredients, drug_class, avoid_with, contraindications, side_effects, warnings, prescriber_id (→ medical_contacts), started_at, active, special_date, special_note, created_at`. Indexed `(profile_id)`. Retention: forever. **`special_date` (DATE) + `special_note` (TEXT)** (added 2026-06-26, migration `013_med_special_schedule.sql`) — an optional **"Special Schedule"** per med: a dated logistical task (order refill, arrange pre-injection authorisation doc, etc.). When any med has it set, the 💊 table shows a **"Special Schedule" column** (`date · special_note`) **colored by proximity** using the **shared project-wide "Schedule & appointment colors"** (`dashboard_settings.privacy.settings` `{red_days,yellow_days,grey_days}`, edited at **Privacy → Settings** — the SAME single config the Privacy appointment cards use; it's purely cosmetic, so one config serves the whole project). Band fn `_phApptBand` in `personal-health.js` is a copy of privacy.js `_pvApptBand` (medical.html doesn't load privacy.js); both read the same settings key, so editing the colors in Privacy recolors both. The Privacy card was relabeled **"Schedule & appointment colors"** to reflect the wider scope.
- **`ph_steps`** — daily step entries: `id, user_id (FK → household_users ON DELETE CASCADE — NOT profile_id; keyed by the person so walking-trip imports match), measured_at (timestamptz), steps (int), source ('manual'|'trip'|'place_leg'), trip_id (bigint — phone_trips.id when source='trip', phone_place_trips.id when source='place_leg'), created_at`. Indexed `(user_id, measured_at DESC)` + **partial UNIQUE `(source, trip_id) WHERE trip_id IS NOT NULL`** (was `(trip_id)` alone until migration `018` on 2026-07-04 — changed so a place-leg id can't collide with a home-trip id; each trip/leg imported once). Migrations `010_steps.sql` + `018_steps_place_legs.sql`. Retention: forever.
- **`ph_body`** — waist & hip circumference log (cm): `id, profile_id (FK → ph_profiles ON DELETE CASCADE), measured_at (timestamptz, server-stamped on Save), waist_cm, hip_cm, created_at`. Indexed `(profile_id, measured_at DESC)`. **WHR (Waist-to-Hip Ratio = waist ÷ hip) is COMPUTED, never stored** — `GET /body` returns `round(waist_cm/NULLIF(hip_cm,0), 2) AS whr`, and the front-end recomputes the latest one for the summary chip. Migration `014_body.sql`. Retention: **forever + `protected=true`** (set directly in the migration — the one-time server.js protect-seed already fired via its sentinel). The per-person measure schedule lives on **`ph_profiles.body_sched`** (JSONB, same shape as `weight_sched`/`bp_sched`).

- **`ph_water`** — daily water intake, **ONE ROW PER (profile, local day) = that day's cup count** (redesigned 2026-07-04, migration `017_water_daily.sql`; was one +1 row per cup summed, which let history edits inflate a day to 24–56). Columns: `id, profile_id (FK → ph_profiles ON DELETE CASCADE), day (DATE, local Asia/Jerusalem, DEFAULT today), measured_at (timestamptz, last-touch — kept aligned to `day` at noon local so date-based reads agree), cups (numeric = the day's total), created_at`. **UNIQUE `(profile_id, day)`** (`uq_ph_water_profile_day`) backs the increment upsert. Migrations `015_water.sql` (create) + `017_water_daily.sql` (collapse to daily + `day` col + unique). Retention: **forever + `protected=true`**. The goal is **GLOBAL** in `dashboard_settings.medical.water` `{enabled, target, start_hm, end_hm, interval_min}` (edited on **Medical → Settings → 💧 Water**) — one household goal/window/interval; each person logs their own cups against it. (`ph_profiles.water_sched` from migration 015 is **unused** — the goal moved global; the column is left in place, harmless.)

- **`ph_exercise_log`** — daily morning-exercise routine log: `id, profile_id (FK → ph_profiles ON DELETE CASCADE), measured_at (timestamptz, server-stamped), total_calories (numeric), muscles (JSONB — distinct list worked), exercises (JSONB — per-exercise snapshot for history), created_at`. Indexed `(profile_id, measured_at DESC)`. Migration `016_exercises.sql`. Retention: **forever + `protected=true`** (set in the migration). **The routine DEFINITIONS are GLOBAL** in `dashboard_settings.medical.exercises` `{enabled, schedule:{time_hm}, items:[{id,name,reps,cal_per_rep,muscles[],other,include}]}` (edited on **Medical → Settings → 🏋️ Morning Exercises**). One row is written here per **✓ Done** (card) or per **reminder Clear**, computed from the include-✓ items by **`BOILER/dashboard/lib-exercise-log.js`** (`logExerciseRoutine(db, profileId)` — `total_calories = Σ reps × cal_per_rep`, muscles = distinct preset keys + non-empty `other`). Shared by `routes-personal-health.js` (Done) **and** `routes-reminders.js` (Clear) so the math never drifts.

All 6 measurement/schedule tables registered in `retention_policies` **and** in the Health → DB-Volumes view. DATE columns are returned as `YYYY-MM-DD` strings (`to_char`) to dodge the pg DATE timezone off-by-one.

## Endpoints (`routes-personal-health.js`, all under `/api/personal-health/`)
- **Profiles**: `GET /profiles` (+ each person's latest weight; returns `weight_sched` + `bp_sched`) · `POST /profiles` · `PATCH /profiles/:id` · `DELETE /profiles/:id` (cascades) — fields `{name, sex, date_of_birth, height_cm, allergies, conditions, weight_sched, bp_sched}`. The two `*_sched` fields are the measure-reminder schedule (JSONB `{freq, interval_n}`/null), PATCHed on-change by the dropdown on the Weight / BP cards (`phSchedChange` in `personal-health.js`).
- **Measurements**: `GET /measurements?profile_id=` (newest first) · `POST /measurements` `{profile_id, weight_kg}` (measured_at stamped server-side on Save) · `DELETE /measurements/:id`.
- **Body (waist/hip)**: `GET /body?profile_id=` (newest first, each row carries computed `whr`) · `POST /body` `{profile_id, waist_cm, hip_cm}` (server-stamped) · `PATCH /body/:id` · `DELETE /body/:id`. Profiles GET also returns each person's `latest_waist_cm` / `latest_hip_cm` + `body_sched`; profiles PATCH accepts `body_sched`. **Waist & hip card** (`#ph-bodycard`, between BP and Steps): teal **Waist (cm)** + purple **Hip (cm)** inputs (color-coded to the diagram) + Save, a **🔔 Measure** schedule dropdown (`phSchedChange('body')`), a **🕓 History** modal (HIST kind `body`, edit/delete + dated add), and an **ℹ️ "Where to measure"** popup (`#ph-body-diagram`) — a static SVG front-view body silhouette with a teal **Waist** line at the narrowest point (navel) and a purple **Hip** line at the widest (hips/buttocks). The person summary gains **Waist / Hip / WHR** chips; WHR is color-banded by sex (WHO cut-points — men 0.90/1.00, women 0.80/0.85 → Low/Moderate/High).
- **Medications**: `GET /medications?profile_id=` · `POST /medications` · `PATCH /medications/:id` · `DELETE /medications/:id` — all `ph_medications` fields. The basic Add/Edit form sets name/dose/schedule/notes; the Info window's `✎ Edit info` PATCHes only the safety fields.
- **Steps**: `GET /steps?user_id=` (→ `{today_total, today_trip, today_manual, today_trip_count}`, Asia/Jerusalem day) · `POST /steps` `{user_id, steps}` (manual, server-stamped, source='manual'). Keyed by `household_users.id`, not `profile_id`.
- **Water** (ONE ROW PER (profile, local day) since 2026-07-04, migration 017): `GET /water?profile_id=` (→ `{today_total}` = today's row `cups`, keyed on `day`) · `GET /water/list?profile_id=&limit=` (one row per day, ordered by `day`) · **`POST /water/inc` `{profile_id, delta?}`** — increment TODAY's row (upsert on `(profile_id, day)`, create if absent; delta default 1, negatives allowed); used by **+1** and the reminder **Clear** · `POST /water` `{profile_id, cups, measured_at?}` — **SET** a day's total (upsert on the local day of `measured_at`, else today; used by the 🕓 history "add", so re-adding a day overwrites it) · `PATCH /water/:id` (sets `cups`; keeps `day` synced when `measured_at` changes) · `DELETE /water/:id`. **💧 Water card** (`#ph-watercard`, Personal Health): **+1 cup** → `phLogWater(1)` (POSTs `/water/inc`), **↺ Reset today** → `phResetWater()` (confirm → `POST /water {cups:0}` → today = 0), a live **"Today: N / target cups"** (green ✓ when met), 🕓 history (HIST kind `water` = daily totals; editing SETS a day), and a **read-only goal line** (`#ph-wt-goal`, from the global cfg). **Goal config = `dashboard_settings.medical.water`** `{enabled, target, start_hm, end_hm, interval_min}`, edited on **Medical → Settings → 💧 Water** (`medWaterSettingsInit`/`medWaterSettingsSave`; cached in `_waterCfg`, loaded in `phInit`). **Reminder** (`routes-reminders.js`): `interval = interval_min || floor(window ÷ target)`; `expected = min(target, floor((now − start) ÷ interval))` within the window; for **every profile**, if `cups_today < expected` (counted via the `day` column, NOT `measured_at`) the badge shows **`💧 Drink water (N/target)`** (`rkey water:<pid>:<date>:<expected-slot>`) — **Clear = +1 cup to today's row** (upsert, then the badge dispatches `ph-water-changed` so the Personal Health card refreshes without a reload); silent outside the window. *(ntfy phone push = a later phase.)* ⚠ **Both the reminder eval AND the history list key off `day`; the migration keeps `measured_at` aligned to `day` (noon local) so any residual `measured_at`-based read still lands on the right date.*
- **Exercise (morning routine)**: `GET /exercise?profile_id=` (→ `{today_calories, today_muscles[], today_count, rows}`, Asia/Jerusalem day) · `GET /exercise/list?profile_id=&limit=` (history) · `POST /exercise/log {profile_id}` (the **✓ Done** button — computes + logs from the global definitions via `lib-exercise-log`) · `POST /exercise {profile_id, total_calories, measured_at}` (manual / backdated history "+ Add") · `PATCH /exercise/:id` · `DELETE /exercise/:id`. **🏋️ Morning Exercises card** (`#ph-exercisecard`, Personal Health) is **compact — today's totals only** (`phLoadExercise` renders just the header): **✓ Done** → `phLogExercise()`, a live **"Today: N cal · M muscles"**, a **🕓 history** (HIST kind `exercise`, editable calories), and an **ℹ️ muscle-map** legend (`phMuscleSvg(keys, w)` — detailed front+back vector anatomy: skin silhouette `_phBody(ox)` + per-group shapes `PH_MUSCLES`, front body ≈x100 / back ≈x300). The **editable routine list + per-exercise muscle thumbnails + the 🧍 Muscle Map picker live on Medical → Settings**, NOT on this card. **Definitions config = `dashboard_settings.medical.exercises`**, edited on **Medical → Settings → 🏋️ Morning Exercises** (`medExSettingsInit`/`medExSettingsSave`/`phExFormSave`; the **+ Exercise** form has name + reps + cal/rep + a **preset muscle multi-select** (Chest…Calves + Full-body) with an **"Other" free-text** fallback + include-✓ + a live diagram preview). **Reminder** (`routes-reminders.js`): daily, due after `schedule.time_hm` (Asia/Jerusalem) when **no `ph_exercise_log` row exists today** AND ≥1 include-✓ exercise; rkey `exercise:<pid>:<date>`; **Clear LOGS the routine** (same `logExerciseRoutine`) — mirror of the water Clear→cup. Muscles are **free-text-safe** but the body diagram only highlights the **preset keys** (`MUSCLE_REGIONS` map in `personal-health.js`); unknown/"Other" muscles show as a text label. **Exercise types + list UI (2026-06-28):** each item has a **Type** (`kind` = `reps` or `hold` timed isometric). **Both Suggested and Real are a full `sets × count`**: **Suggested** = `suggested_sets × suggested` (recommendation, rendered **green**), **Real** = `sets × reps`/`hold_sec` (id `setx-sets` × `setx-reps`/`setx-hold` — what you actually do). Calories use the **Real** side: reps×sets×cal_per_rep (or hold_sec×sets×cal_per_sec); `suggested*` are reference-only. The Settings **+ Exercise** form is 2 rows (row 1 settings / row 2 definition, **+ Add leftmost on row 2**: name · Type · Suggested `[sets]×[count]` · Real `[sets]×[count]` · cal/rep-or-sec). The exercise list (`#setx-list`) **auto-groups into collapsible body-area sections in **fitness-flow order with a numbered badge (workout order ①②③…)**: **🏃 Warm-up / Cardio → 🦵 Legs → 💪 Upper Body → 🎯 Core → 🧠 Cool-down (Head & Neck) → Other** (`_AREA_META` order; `_AREA_OF` map + `_exAreaOf` = the section holding the MAJORITY of an exercise's muscles, tie→first muscle's area). The badge number is consecutive over the VISIBLE sections (① = do first, highest = last). **🏃 Warm-up / Cardio is PINNED — always shown** (even empty, with a placeholder) so future warm-up exercises have a home. An exercise lands in a section either when its majority muscle resolves there, OR via an explicit **`section` override** — an optional `section` field on an item (e.g. `section:'cardio'`) that **forces** it into any `_AREA_META` section regardless of muscles, **while KEEPING its real muscles for the diagram** (`_exAreaOf` checks `it.section` first if it's a valid `_AREA_META` key, else the muscle-majority math). Used for **Running in Place + Standing High Knee Raise → Warm-up** (still highlight legs/core on the map — avoids the `fullbody`-tag whole-body-highlight). **Order follows ACSM/NSCA** (warm-up → compound lower-body before upper → core as a finisher → cool-down last); within a section, exercises are arranged compound/most-demanding first, **isometric holds last** (e.g. Hollow Body Hold ends Core). Each section is a ▾/▸ header with a count; **show/hide persisted in `localStorage` `phExSections`** (default expanded). Rows: `☑` first, Name+green-detail on one line, muscle thumb, small Edit/✕, **`⋮⋮` drag handle** — **drag-reorder is scoped WITHIN a section** (cross-section drops blocked, since an exercise's section is fixed by its muscles). In the detail line the **`real …` segment is green when it matches Suggested, red when it deviates in COUNT or SETS** (each side judged only when a suggestion exists, so legacy items with no `suggested_sets` aren't falsely flagged). The **Muscle Map** figure wrapper has `min-height:240px` so the front+back figure never shrinks when the card is short. **Personal Health profile-card stats (2026-06-28):** the profile card header (`#ph-ex-stats`, inline after Edit details) shows **3 big green per-person TODAY totals — exercises done · total time · calories**; `GET /exercise` returns `today_ex_count` (Σ exercises across today's rows) + `today_seconds` (hold = `hold_sec×sets`, reps estimated at ~3 s/rep). The Morning Exercises card keeps its compact `Today: N cal · M muscles` line. **Settings-card routine totals (2026-06-28):** the **Settings** 🏋️ Morning Exercises card shows the routine totals from the **REAL values** in its **top-right corner in BLACK** (`#setx-totals`, `_exRenderTotals`) — **# exercises · time · calories** of the *included* items, recomputed on every list change (add/edit/delete/include-toggle/drag). Distinct from the green per-person TODAY stats on the profile card (which read logged `ph_exercise_log` rows); the Settings totals are the *defined routine's* projected totals, not what was logged. **PLANNED (not built):** a future rule (LXC 105) / scheduled script (LXC 104) reads the person's full medical picture (`ph_profiles` age/conditions, weight, `ph_*` trends, tests, meds) and auto-recomputes each `items[].suggested` (optionally via an AI call) back into `medical.exercises` — dashboard stays UI-only; the green/red Real coloring already surfaces the resulting drift. Heuristic reference, not medical advice. See memory [[personal-health-exercises]].

## Daily steps (manual + walking-trip import)
A **👟 Daily steps** card in the same grid row as Log weight / Log blood pressure: a number input + **Save** (server-stamped) and a live **"Today: N steps (M from K walking trips)"** line, fed by `GET /steps`. Keyed by the selected person's `household_users.id` (so manual + trip entries share a key).

**Auto-import — SEGMENT-BASED classifier (rewritten 2026-06-27)** — `scripts/steps_from_trips.py` on **LXC 104**, cron `*/15`. For each candidate — a confirmed closed `phone_trips` row (`source='trip'`) **or, since 2026-07-04, a Places-layer `phone_place_trips` leg (`source='place_leg'`)** — not yet imported, it **pulls the trip's actual GPS points** from `device_locations` (mapping `group_id → device_id` via the `dashboard_settings.geolocation` `tracked_devices`) and decides **walk / drive / phantom from PER-POINT segment speeds** — NOT the GPS-jitter-inflated `path_length_m` (which read a normal walk as "too fast" and couldn't tell a steady walk from drive→park→drive). Per consecutive pair it keeps only **good-accuracy segments** (both endpoints `accuracy_m ≤ accuracy_gate_m`, default 35) → `clean_m` (de-noised walked distance) + a list of segment km/h. Classify: **phantom** if `clean_m < phantom_min_m` (default 150) → skip; **driving** if `p85(speeds) > drive_kmh` (default 15) **OR** `≥ drive_run_segs` (default 3) consecutive segments `> drive_kmh` → skip (catches both a mostly-moving drive and a drive→park→drive burst); **too far** if `clean_m/1000 > walk_max_km` → skip; else **walking**. **GPS-glitch guard (`glitch_kmh`, default 45, 2026-07-05):** a segment faster than this between two ADJACENT pings is a physically-impossible GPS teleport — its phantom distance is **dropped from `clean_m`** AND it **breaks the consecutive fast-run** (but stays in the p85 pool, so a genuinely sustained fast drive still trips p85). This fixes the bug where a brief GPS blip made a real walk read as a car — it skipped trip **13212**'s 3.6 km morning walk on 2026-07-05 (5 impossible segments incl. 124 m/4 s = 112 km/h and 44 m/1 s = 159 km/h → false `max_run`); after the guard it correctly imported 3,438 steps → `steps = round(clean_m/1000 × steps_per_km)`, `measured_at = trip.returned_at`, mapped to a member by `device_label → household_users`. **Trips with no stored points (aged out / wiped, or group not mapped) are SKIPPED — never guessed.** Idempotent (partial-unique `(source, trip_id)` + NOT-EXISTS). **Place-leg support (2026-07-04) is PURELY ADDITIVE — the classifier is UNCHANGED**, so the driving legs of an away-trip (`home_to_place`/`place_to_home`, the ~16 km drives) auto-skip and only genuine `place_to_place` walks import; e.g. on 2026-07-04 leg #56 (507 clean m, p85 4.7 km/h) → 685 steps, while leg #57 (jittery, p85 34.6 km/h, 4 fast segs) correctly stayed a **drive** (skipped) — catching #57 too would need softening the shared drive rule, rejected as it would miscount real short drives. Validated on a real walk (trip 13108, 2026-06-27): clean 2598 m, p85 ~8 km/h, max seg 13.7 (0 > 15) → walking → 3507 steps (vs the old jitter guess 2937). The plan/old-jitter history is in memory [[project_steps_segment_classifier]]. (Only Vadim's phone produces trips today; Maya gets trip-steps once her phone is OwnTracks-tracked.)

**Reconcile each run** — before importing, `DELETE FROM ph_steps WHERE source='trip' AND NOT EXISTS (… phone_trips …)` (plus the parallel `source='place_leg'` vs `phone_place_trips`, since 2026-07-04) drops any trip-step whose trip the `geo_trip_janitor` (LXC 104, `*/5`) later deleted, keeping `ph_steps` trip rows in sync with the authoritative `phone_trips` set. (The **pre-2026-06-27 method** — `path_length_m/duration` speed window + a `min_trip_dist_m` / jitter-% discount — is gone; the segment classifier supersedes both the speed-window and the phantom-distance filter. Old `ph_steps` rows from that era keep their values until manually edited/removed; trips whose points have since aged out can't be recomputed.)

**Timezone (2026-06-26):** all three history lists + the "✓ saved" status (weight / BP / steps) render `measured_at` via `to_char(measured_at AT TIME ZONE 'Asia/Jerusalem', …)` — the pg session is `Etc/UTC`, so the bare `to_char` showed times 3 h behind local (e.g. a 09:59 trip read 06:59) while the "Today" total — which already converted — was right. The 6 `GET`/`POST` renders in `routes-personal-health.js` now convert. Stored instants are true UTC (OwnTracks `tst` epoch; manual saves = `now()`); only the **display** was wrong. The `+Add`/Edit round-trip is symmetric: `_histTs` builds a UTC ISO from the local date+time, display converts back. (Tests tab renders `tested_at` browser-side via `new Date(iso).toLocaleDateString` — different method, also correct.)

**Medical → Settings tab** edits the thresholds, stored in `dashboard_settings.medical.steps`: `steps_per_km` (def 1300), **`accuracy_gate_m`** (def 35), **`phantom_min_m`** (def 150), **`drive_kmh`** (def 15), `walk_max_km` cap (def 30), **`history_limit`** (def 10). (`drive_run_segs` def 3 and **`glitch_kmh` def 45** (2026-07-05 GPS-glitch guard) are code defaults, overridable via the JSON but not surfaced in the UI.) Read by the watcher each run. The old `walk_min_kmh`/`walk_max_kmh`/`jitter_pct` knobs were removed 2026-06-27 with the speed-window method. (Manual + trip steps both add to the daily total — by design; if the manual number comes from a phone pedometer it already includes the walks, so that's a deliberate double-count the user accepts.)

**History modal (view / edit / remove — all three cards, 2026-06-26):** a **🕓 icon** on each of **Log weight**, **Log blood pressure**, **👟 Daily steps** opens a shared modal (`#ph-hist-modal`, JS `HIST`/`phHistory`/`phHist*` in `personal-health.js`) listing the **last N entries** (N = `history_limit`) for the selected person, each with inline **Edit** + **✕ remove**, plus a **"+ Add"** form that accepts a **chosen timestamp** (date + time, default now → ISO) for backdating / re-adding. **Trip-derived step entries are fully editable** (the km→steps calc isn't always right; the importer never overwrites an existing row, so an edit sticks) **and removable** — deleting a trip entry inserts its `trip_id` into **`ph_steps_excluded_trips`** so `steps_from_trips.py` won't re-import it (`AND t.id NOT IN (SELECT trip_id FROM ph_steps_excluded_trips)`). Backing endpoints (all in `routes-personal-health.js`): weight `GET /measurements?…&limit=` + `PATCH /measurements/:id` + `DELETE`; BP `GET /bp?profile_id=&limit=` + `PATCH /bp/:id` + `DELETE`; steps `GET /steps/list?user_id=&limit=` + `PATCH /steps/:id` + `DELETE /steps/:id` (the delete adds the trip exclusion). All three `POST`s accept an optional `measured_at` (ISO) for the timestamped add. New table **`ph_steps_excluded_trips`** (`trip_id PK, excluded_at`) — migration `011_steps_excluded.sql`. Retention **forever** + registered in **Health → DB-Volumes** (`tsCol = excluded_at`) since 2026-06-26.

## Computed metrics (front-end, `personal-health.js`)
- **Age** = years from `date_of_birth`
- **BMI** = `weight_kg / (height_cm/100)²`; **category** color-coded (Underweight `#e67e22` / Normal `#2e7d32` / Overweight `#e67e22` / Obese `#c0392b`)
- **Ideal weight** range = BMI 18.5–24.9 mapped to kg for the person's height
- **WHR** = latest `waist_cm ÷ hip_cm` (2 dp); **category** by sex (WHO: men Low `<0.90` / Mod `<1.00` / High; women Low `<0.80` / Mod `<0.85` / High), color-coded green/orange/red

## Deploy notes
- HTML/JS are static → cache-bust + hard-refresh.
- `routes-personal-health.js` is a backend route module → needs a **dashboard restart** (`pm2 delete boiler-dashboard && pm2 start ecosystem.config.js`) to register the routes.
- The migration runs on LXC 102 via LXC 104 (`psql -h 192.168.1.219`).

## 🛹 Bobo card (2026-07-07; storage moved to `ph_bobo` same day)
Per-person balance-game card, mirroring "Log weight". BoBo is a Personal Health **activity** (like BP / water /
steps / exercises), so its games live in their **own table `ph_bobo`** keyed by **`profile_id`** — NOT in
`medical_test_results` as a "test" (the original wiring; the user flagged that it wrongly showed under Medical →
Tests). **`ph_bobo`** cols: `profile_id` FK ph_profiles, `measured_at`, `game`, `level`, `score`, `duration_s`,
`calories`, `details` jsonb, `created_at`; index `(profile_id, measured_at DESC)`; retention forever+protected.
Migration **`020_bobo.sql`** creates it, migrates the old `test_type='balance'` rows over (mapping user_id→profile),
and DELETEs them from `medical_test_results`. **Endpoints `/api/personal-health/bobo`** (`routes-personal-health.js`):
GET `?profile_id=` = one person's history / no id = all-players recent (game menu); POST resolves `profile_id` from
`user_id` (the TV only knows the household user); DELETE. GET returns the old row shape (`results` blob + `meta.game`
+ `member_name`) so the shared game shell + `renderBalance` work unchanged. **Burned calories** computed on save in
the LXC `bobo_game_service.py` `POST /api/bobo/score` = `MET(level) × latest ph_measurements.weight_kg ×
duration_s/3600` (MET 2.5/3.0/3.5 easy/med/hard; skipped if no weight), first-classed into `ph_bobo.calories`. The
card (`phLoadBobo(p)` + `#ph-bobocard`) shows totals (games · time · 🔥cal) + a **🕓 history** (view via
`renderBalance` / delete, like BP). Both writers (LXC `/api/bobo/{score,recent}` + dashboard `bobo-game.js`
`EP.{saveScore,recent}`) point at `ph_bobo`; the `balance` branches were removed from `medical-hearing.js` (out of
Tests). **🔔 Play schedule** — `ph_profiles.bobo_sched` jsonb (migration `019`), SAME `{freq, interval_n}` shape.
Reminders (`routes-reminders.js`) shows a **"🛹 Bobo"** pill when no game within the window (`max(measured_at) FROM
ph_bobo WHERE profile_id`; was reading the emptied balance tests → would fire forever). Schedule helpers reused by
registering `bobo` in the hardcoded `_schedPre` + `phSchedChange` maps. Full BoBo pipeline
(bridge/game/calibration/TV auto-switch): `project-bobo-balance-bridge`
memory + `BOILER/dashboard/bobo-lxc/`.

## Extending
Add the deferred metrics by extending `ph_measurements` (waist_cm, resting_hr, bp_*) + `ph_profiles` (activity_level, target_weight_kg), then the front-end derivations (BMR/TDEE/body-fat) + a Chart.js progress card + a goals card. Steps/distance would be a sibling table `ph_activity_log`.
