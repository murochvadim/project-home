# Privacy Agent

**Status: BUILDING (started 2026-06-13).** Phases 1–5 done: LXC + Docker + Vaultwarden (hardened) + Cryptomator docs vault in Google Drive + LXC copy + nightly QNAP backup. Phases 6–7 (phone/laptop clients, dashboard tile) pending.

### Built so far (2026-06-13)
- **LXC 109 `privacy`** on Proxmox host `192.168.1.101` — Debian 12.12, 2 vCPU / 2 GB / 16 GB, **static `192.168.1.196`**, privileged + `features: nesting=1,keyctl=1` (for Docker), `onboot=1`, SSH via the PVE `claude-code` key. Created with `pct create … local:vztmpl/debian-12-standard_12.12-1_amd64.tar.zst`.
- **Docker** 29.5.3 + compose v5.1.4 (official docker.com repo).
- **Vaultwarden 1.36.0** (≥ 1.35.5 ✓) + **Caddy** (HTTPS, internal CA) via `docker compose` at `/opt/privacy/` → repo copies: [docker-compose.yml](docker-compose.yml) + [Caddyfile](Caddyfile). URL **`https://192.168.1.196`**.
  - Hardening applied: `SIGNUPS_ALLOWED=false` (locked after the 1 account `murochvadim@gmail.com` was created — register now returns 422), `ORG_CREATION_USERS=none` (orgs disabled → removes the 2026-CVE surface), **no `ADMIN_TOKEN` → `/admin` disabled**.
  - **HTTPS gotcha solved:** serving on a bare IP failed TLS (`tlsv1 alert internal error`) because clients can't send an IP as SNI → fixed with Caddy global `default_sni 192.168.1.196`.
  - **Caddy internal root CA** at `/opt/privacy/caddy-data/caddy/pki/authorities/local/root.crt` (copied to the Windows host `C:\Users\muroc\caddy-root-CA.crt`) — **install on each device** to trust the cert.
- **User DONE:** KDF = **Argon2id** (64 MB/3/4, verified in DB); Caddy root CA installed in the laptop's Trusted Root (HTTPS lock clean). Data volume `/opt/privacy/vw-data` (NOT in git — vault DB).
- **Cryptomator docs vault (Phase 4, 2026-06-13):** vault created by the user on the laptop at **`G:\My Drive\Privacy`** (named "Privacy") via the Cryptomator desktop app (v1.19.2) + WinFsp. Encrypted (client-side AES-256); Google Drive desktop syncs the ciphertext to the cloud + everywhere. Vault password is the user's alone (stored in Vaultwarden, never with the assistant) — separate from the Vaultwarden master password. To use: Cryptomator → Unlock → mounts a drive → drop files in.
- **Read-only Drive access (Phase 4c):** `rclone` remote `gdrive` with **`scope = drive.readonly`** (OAuth token obtained via `rclone authorize` on the laptop, browser consent). Token copied to **LXC 109** (`/root/.config/rclone/rclone.conf`) AND **LXC 104**. Read-only = the servers can pull the vault but can NEVER modify/delete anything in the user's Drive. LXC 109 holds a manual ciphertext copy at `/opt/privacy/vault-copy/Privacy`.
- **Nightly QNAP backup (Phase 5):** `scripts/privacy-vault-backup.sh` → `/opt/privacy-vault-backup.sh` on **LXC 104** (per the timers-on-104 rule). `rclone sync gdrive:Privacy → /mnt/qnap-claude/Privacy_Vault`, cron **`15 3 * * *`**, log `/var/log/privacy-vault-backup.log`. Ciphertext only (no vault password). **3 copies of the encrypted vault:** Google Drive (primary/offsite/view-anywhere) + LXC 109 (manual snapshot) + QNAP (nightly automated). Verified: backup ran, copy landed on QNAP.
- **Encrypted project-folder backup → Drive (2026-06-29):** an off-site, **encrypted** copy of the WHOLE `project_home` repo (incl. the secret `BOILER/dashboard/.env`) on Google Drive. `scripts/privacy-project-cloud-backup.sh` → `/opt/` on **LXC 104**, cron **`30 3 * * *`**, log `/var/log/privacy-project-cloud-backup.log`. **Chains onto the existing Windows backup** — it tars the newest QNAP snapshot the "Claude Project Folder" `backup_jobs` row already made (`/mnt/qnap-claude/Claude Project/<newest>/project_home`, so NO new laptop access), **excluding `node_modules` + `.git`**, then `gpg --symmetric --cipher-algo AES256` (passphrase in **`/etc/privacy-project-backup.pass`**, root-only `chmod 600`, also kept in **Vaultwarden**), then `rclone copyto` a dated + `_latest` `.gpg` to **`gdrive_sheets:Claude_Project_Enc/`** (reuses the **`drive.file`-scoped** remote → still can touch ONLY its own files), prunes dated > 14 d, and records its success time in `dashboard_settings.privacy.project_backup`. **~2.2 MB** encrypted. ⚠ Unlike the Budget (key only in the browser), this runs unattended so the **passphrase lives on LXC 104** (root-only) + Vaultwarden — Google still sees only ciphertext. **Verified:** ran OK, file in Drive, **decrypt test passed** (recovers `project_home/` incl `.env`, `node_modules` absent), scope still restricted. **Restore:** `rclone copy` the `.gpg` back → `gpg --decrypt --passphrase-file …` → `tar xzf` → `npm install`. **Recent Backup Log integration (2026-06-29):** BOTH Drive scripts (budget + project) also write a `backup_log` row each run (running → ok/failed with size), so all four runs show together on Project Health → Recent Backup Log. They log against two **disabled** `backup_jobs` rows — **"Privacy Budget (Drive)"** + **"Project Folder (Drive)"** (`enabled=false` so `backup-script.sh` skips them — they exist only to give the Drive runs a name in the INNER-JOINed `/api/backup/log` query); the scripts look the job up by name (no hardcoded id). **Backup Storages surface (2026-06-29):** the `Claude_Data` storage description gained `+ Google Drive (encrypted)` (DB edit; `Windows_Data` left as QNAP-only — it's image backups, not on Drive), and the **Backup Storages card header** (`health.html`) shows a **☁️ Backups** strip (🏠 Home = last "Claude Project Folder" QNAP backup, ☁️ Cloud = last project Drive backup, 📁 Open in Drive → the `Claude_Project_Enc` folder) fed by **`GET /api/privacy/project-backup-status`** (`routes-privacy.js`) + `loadStorageCloudStatus()` in `js/health.js` (cache-bust `v=57`).
- **Restore tab (Project Health, 2026-06-29):** a **Restore** tab (right of Windows Backup) that recovers BOTH the **project folder** and the **Budget** — SAFE by design. `scripts/restore-backup.sh` → `/opt/` on **LXC 104** (has the QNAP mount, the `gdrive_sheets` remote, the gpg passphrase, DB access, SSH to the laptop). **Project restores ALWAYS land in a NEW timestamped folder** (`/mnt/qnap-claude/Restores/` = `\\192.168.1.155\Claude_Data\Restores\`, or the laptop `C:\Users\muroc\Restore\` via scp) — the script hard-refuses the original `project_home` path; source = a QNAP snapshot (`cp`, full incl node_modules) OR the encrypted Drive `.gpg` (`rclone copy`→`gpg --decrypt`→`tar xzf`, no password — uses the server-side passphrase). **Budget restore** has no "new location" (it's the single `privacy_sheets` DB row), so it's safe by **rollback**: it snapshots the current blob to `dashboard_settings.privacy.budget_rollback` FIRST, then writes the chosen backup blob (QNAP or Drive) into `privacy_sheets` — **ciphertext only, no password** (you unlock in the Budget tab afterward to view). Backend = **`routes-restore.js`** (own module, one `require()` line): `GET /api/restore/sources` (lists project+budget backups, QNAP+Drive) + `POST /api/restore/run {type,source,ref,dest}` — **refs are validated against strict patterns (`TS_RE`/`PROJ_FILE_RE`/`BUD_FILE_RE`) and the full path is built server-side, so no shell injection** (verified: `ref:"x; rm -rf /"` → `bad ref`). UI: `js/health.js` `loadRestoreSources()`/`doRestore()` (confirm dialog per restore; cache-bust `v=58`). All restore modes verified end-to-end incl. a real Budget restore (live blob matched the backups byte-for-byte → decryptable; rollback saved). **DB + Guests sections added 2026-06-29:** the Restore tab now also lists the **Database** Drive dumps with a **safe "Restore ▸" → into a NEW `home_data_restore` database** (script `db` type: `rclone cat | gpg --decrypt | pg_restore` with the v16 client; drops+recreates ONLY `home_data_restore`, **never the live DB** — verified: restore made `home_data_restore` with 79 tables while live `home_data` stayed at 79, untouched), and a **Guest Images** card that is **visibility-only** (latest off-site copy per guest 100–109, no restore button — guests restore natively via Proxmox from QNAP; the Drive copies are disaster-only). `GET /api/restore/sources` extended with `database.drive` + `guests`; `POST /api/restore/run` gained the `db` type (Drive-only, `DB_FILE_RE`-validated, dest forced to the scratch DB — injection/wrong-source/bad-type all rejected). Cache-bust `health.js?v=60`.
- **Database + all-guests off-site backup (2026-06-29):** two more encrypted Drive backups via the same `gdrive_sheets` (`drive.file`) remote + gpg, both **fully streamed** (`… | gpg | rclone rcat`, no temp file — LXC 104 rootfs is 8 G). **(DB)** `scripts/db-cloud-backup.sh` → `/opt/`, cron **`40 3 * * *`** (nightly): `pg_dump -Fc` **using the v16 client** (`/usr/lib/postgresql/16/bin/pg_dump` — installed because the server is PG16 and the stock v15 client refuses a newer server; PGDG repo added to LXC 104) → gpg → `gdrive_sheets:DB_Backups/` (dated + `_latest`). **~145 MB** (the 3.5 GB DB → 148 MB `-Fc` dump). Verified restorable (decrypt → `pg_restore -l` lists 79 tables). **(Guests)** `scripts/guests-cloud-backup.sh [id]` → `/opt/`, cron **`0 4 * * 0`** (weekly Sun): for each guest **100–109** (LXCs `.tar.zst` + the **HA VM 101** `.vma.zst`, ~**12.3 GB** total, the VM being 3.9 GB) take the newest vzdump from a **read-only bind-mount of the QNAP `/PBS_Data` root** (`pct set 104 -mp2 /mnt/pve/QNAP_Backups,mp=/mnt/pbs,ro=1` — read-only, can't alter backups; needed a one-time LXC-104 reboot) → gpg → `gdrive_sheets:Guest_Images/<id>/`. Optional arg backs up a single guest. Verified (guest 108: decrypt → zstd magic `28 b5 2f fd`). Both record `dashboard_settings.privacy.{db_backup,guests_backup}` + log to Recent Backup Log via disabled jobs **"Database (Drive)"** + **"Guest Images (Drive)"**. **Retention is settings-driven:** `dashboard_settings.privacy.cloud_retention = {project_days:14, budget_days:30, db_days:30, guests_weeks:4}`, edited on **Project Health → Settings → ☁️ Cloud Backup Retention** card (`loadCloudRetention`/`saveCloudRetention` in `js/health.js`, clamped to **min 1** so a typo can't wipe everything); **ALL FOUR** off-site scripts read their value at runtime (the existing project + budget scripts were rewired to read it too). The Drive holds **5 TB** (not the 15 GB free tier — verified via `rclone about gdrive:`), so retention cost is negligible. **The "Windows Backup" tab was renamed → "Backup"** (label only; internal id stays `win-backup`) since it now covers QNAP + all four off-site Drive backups. Cache-bust `health.js?v=59`.
- **Pending (Phases 6–7):** install Bitwarden + Cryptomator + Caddy root CA on the **phone** (and Bitwarden extension on the laptop). Phone = user opted out (2026-06-13).

## Dashboard page — Privacy (Personal group, first; since 2026-06-13)
`BOILER/dashboard/public/privacy.html` + `js/privacy.js`, sidebar **Privacy** as the **first item in Personal** (above Medical). Backend: **`routes-privacy.js`** (own module, architecture-guard pattern). Tables on LXC 102: `privacy_sites`, `privacy_site_docs`, `privacy_doc_crypto` (migration `PRIVACY/migrations/002_privacy_dashboard.sql`).

**Tab: Sites** — a CRM of services/accounts (bank, insurance, gov…). Each site: kind / name / Main Tel / **Additional phones** (list of `{tel, person}`, editable) / fax / email / website / optional **Vault item** (powers a **🔑 Vaultwarden** link that opens `https://192.168.1.196`) / notes. Add/edit (modal) + delete. Stored plaintext (LAN-only, like `medical_contacts`). Also a **📅 Next appointment** (date + HH + MM 24h inputs + reason; `next_appointment_at` TIMESTAMPTZ + `next_appointment_note`) and a **🔔 Reminder** (standalone text; `reminder_text`) — same pattern as Medical Contacts: red appointment mini-card (turns muted + "past" pill once elapsed) + blue reminder mini-card on the site card; date+HH+MM combine to a UTC ISO client-side (`_pvPartsToISO`). **Appointment color bands** (since 2026-06-15) — the single appointment card is *colored by proximity* (still one appointment + one reminder; no `!` square — that earlier same-day idea was replaced). A far-off appointment is 🟢 **green**; as it approaches it turns 🟡 **yellow** then 🔴 **red**. After the appointment day it **stays red** for `grey_days` more days, then turns ⚪ **grey forever**. The **Settings** tab has **3** day inputs: `red_days` = days right before the appointment (red), `yellow_days` = days before red (yellow), `grey_days` = days it stays red *after* the appointment before going grey. 🟢 green is the automatic catch-all for anything further out than yellow (no far cutoff — a 76-day appointment is green). Stored in `dashboard_settings` key `privacy.settings` = `{yellow_days,red_days,grey_days}` (defaults 7/3/7) via the generic `/api/dashboard-settings/:key` GET/POST — **no privacy backend / DB change**. **Layout (since 2026-06-15):** the appointment + reminder sit in a **full-width row below the name/details + action-buttons top row**, in a `flex:1; align-items:center` wrapper so the block is **vertically centred in the middle of the card** (the card is `display:flex; flex-direction:column`). Horizontally the row is a **3-column grid `minmax(0,1fr) 200px minmax(0,1fr)`**: the 📅 **appointment is ALWAYS dead-centre** (centre column, **fixed 200px** = the appointment width, so it stays reserved even when there's no appointment); the 🔔 **reminder**, when present, sits in the **left** column right-aligned (immediately left of the appointment slot); the empty right column is a balancing spacer. Because the centre column is a fixed width (not `auto`), the **reminder's position is constant** — it does not slide toward centre when the appointment is absent (fixed-`auto` was the bug: an empty `auto` centre column collapsed to 0 and shifted the reminder right). The reminder is `max-width:100%` so on a narrow card it shrinks within its column instead of clipping (same size as the appointment when there's room). With the two cards in a grid row stretched to equal height, the rows line up. To make the centred appointments **line up across the two neighbour cards**, `pvRenderSites` does a post-render pass that equalises each side-by-side pair's **top-section height** (`firstElementChild.minHeight = max of the pair`) — so the space below (where the appointment centres) matches and both appointments sit on the same level. The two mini-cards are **200 px** wide (`_pvApptReminderArea` + `pvRenderSites`). **Styling:** card text is always **black**; every card (grey/red/yellow/green + the reminder) is **frameless** (border == fill). Logic: `_pvApptBand(date)` → `_pvApptCard` styles bg/border; `pvLoadSettings()` runs in `pvRefresh`, `pvSaveSettings()` re-renders sites on save. (Earlier same-day iterations — a "flip to `!` square", then a 4-band model with `green_days`+`grey_days` widths and a plain far/long-past zone — were all replaced; stale `green_days`/`grey_days` may linger in the saved JSON until the next Save, harmless/unread.)

**Drag-to-reorder sites** (since 2026-06-15) — each site card has a **⋮⋮ handle** (top-left, by the kind/name); drag a card onto another to reorder (dashed-blue drop highlight). Persisted in a new `privacy_sites.sort_order INTEGER` (migration `002`); `POST /api/privacy/sites/reorder {order:[ids]}` writes `sort_order = position`; GET orders `sort_order ASC NULLS LAST, kind, name` (new sites fall to the end). Only the handle is `draggable`; wiring (`pvWireSiteDrag`) is skipped while a filter is active. Mirrors power.js `mdWireDragReorder`, adapted for the 2-col card grid.

**Documents card launchers** (Sites tab top row, since 2026-06-14) — `pvRenderLockState()` renders, to the right of the lock action button (Set password / Unlock / Lock), two same-size `btn-sm` launchers: green **🔑 Vaultwarden** → `https://192.168.1.196` and dark (`#222`, the card name color) **📁 Google Drive** → `https://drive.google.com/drive/u/0/my-drive`. Plain `window.open` links — no vault access (password CRUD stays in the native Bitwarden clients by design).

**Per-site 📄 Docs window** — each site holds documents, each **🔒 encrypted OR 🔓 plain** (user chooses per doc at add time). Add / open / rename / delete; PDFs+images preview in a new tab, others download.

**🔗 Plain links (since 2026-06-18)** — the same Docs window also holds **links** (e.g. a Google Drive folder URL): a `kind='link'` row in `privacy_site_docs` with the URL in a new `url` column, `encrypted=false`, **no file, no crypto** (a link is just a plaintext URL, so it deliberately bypasses the encryption layer — that's the whole point: it points at Drive). Add via a "🔗 Add link" form (name + URL) in the docs modal; rows render with a 🔗 icon and **click opens the URL** (no 🔒/🔓 toggle — links are always plain). Backend: `POST /api/privacy/sites/:id/links {name,url}` (JSON, no multer); `GET …/docs` returns `kind`+`url`; `PATCH /docs/:id` accepts `url`; **DELETE needs no change** — a link's `file_path` is `''` so the existing guard skips the QNAP SSH file-delete and just drops the row. Migration `003_doc_links.sql` adds `kind TEXT DEFAULT 'file'` + `url TEXT` (file_path stays NOT NULL; links store `''`). JS: `pvAddLink`/`pvEditLink`/`pvOpenLink` in `js/privacy.js` (`?v=66`).

**📁 Paths (since 2026-06-24)** — a **3rd doc kind** alongside file + link: a **filesystem / network path** (e.g. `\\192.168.1.155\share\contract.pdf`) pointing at where a document physically lives — **no upload**, plaintext like a link. Stored as `kind='path'` with the path in the existing **`url` column**, `file_path=''` — **no schema change** (reuses the `003_doc_links.sql` shape; GET/PATCH/DELETE already handle it: list returns `url`, rename PATCHes `url`, DELETE skips the QNAP rm because `file_path=''`). Add via a "📁 Add path" form (name + path) in the docs modal; rows render with a 📁 icon + the path text and **Copy / Open / Edit / Del** buttons (uniform size, Copy first). **Copy** → clipboard (paste into Explorer). **Open** → `POST /api/privacy/docs/:id/open` runs `explorer.exe "<path>"` on the **Windows host** (the dashboard runs on the laptop) via `execFile` — path passed as a **single arg (no shell → no injection)**, gated to `kind='path'` only, and the server binds `127.0.0.1` so it's callable only from the laptop. Opens the file in its default app / the folder in Explorer, **on the laptop** (not on a remote viewer's device — browsers block `file://`/UNC from a page, which is exactly why **Copy** is the fallback). Backend: `POST …/sites/:id/paths {name,path}` + `POST …/docs/:id/open` in `routes-privacy.js` (gained `const { execFile } = require('child_process')`). JS: `pvAddPath`/`pvCopyPath`/`pvOpenPath`/`pvEditPath` in `js/privacy.js` (`?v=70`). Adding either endpoint needed a dashboard restart (`pm2 delete + start`) to register the route.

**Encryption — server is BLIND, all client-side in `js/privacy.js`:**
- **One "Documents password"** for the whole page (stored in Vaultwarden; forget it = encrypted docs unrecoverable). Per-session unlock; Lock button + lock-state indicator.
- **WebCrypto AES-256-GCM**, key from **PBKDF2-SHA256 @ 600,000 iters** (OWASP 2025/2026-verified) + a random 16-byte salt; per-file random 12-byte IV. `privacy_doc_crypto` (singleton) stores ONLY the salt + a **verifier** (a known token encrypted with the key) → lets the page detect a wrong password without being able to decrypt anything. The password/key/plaintext/real filename **never reach the server**.
- Encrypted docs: the **filename is encrypted too** (`enc_name`) — the real name is stored as `displayName||originalFilename` so the file TYPE is derived from the decrypted name on open (not leaked). File bytes uploaded as ciphertext.
- Plain docs: stored as-is with plaintext name + mime.
- **Storage:** file bytes on **QNAP** `\\192.168.1.155\Claude_Data\Privacy_Site_Docs\` (ciphertext for encrypted, as-is for plain); **DELETE tunnels via LXC 104 SSH** (same QNAP-ACL reason as Medical Documents). 25 MB cap (30 MB multer headroom). Verified end-to-end 2026-06-13.

**Tab: Doc Create** (since 2026-06-14) — generate a document from a **template ("kind")**, sign it, and print/save it. Logic in `js/privacy-doccreate.js` (`pvdc*`); all client-side, no new backend endpoint (save reuses the Sites docs upload). Vendored libs in `public/vendor/doccreate/`: `signature_pad.umd.min.js` (hand-drawn signature, v4 — `addEventListener('endStroke')`) + `html-docx.js` (HTML→DOCX, `window.htmlDocx.asBlob`).
- **Engine history — html2canvas/jsPDF were tried and REMOVED.** `html2pdf.bundle.min.js` (html2canvas) mangled RTL Hebrew every way (right-edge clipped, dropped spaces, phantom `<table>` row gaps, blank/empty canvas via `foreignObjectRendering`, half-rendered signature box). A `jsPDF` + embedded-Alef-font vector path was then built for real PDF-blob-to-site, but its manual BiDi mixed fields and was abandoned per user. **Final design: PDF via the browser's NATIVE print engine** (`pvdcPrint()` opens a new window with the doc HTML + `@page` CSS + `print-color-adjust:exact` so backgrounds survive Save-as-PDF, then `window.print()`), and **DOCX (`html-docx-js`) for saving into a site**. A correct PDF *blob* can't be produced reliably client-side, so PDFs go to disk via Print; sites store the editable DOCX.
- **Templates** — `PVDC_TEMPLATES` map; each entry = `{label, fields[], title, fileName(d), html(d, sigHtml)}`. Add a template by adding one entry + an `<option>` in the kind `<select>`. First template **`bank_transfer`** = Hebrew **בקשה לביצוע העברה בנקאית** (RTL form: date / requester name / ID / beneficiary / target bank / branch / account / amount + amount-in-words / purpose). Rendered as a styled RTL document; the field grid is **flex `<div>`s, not a `<table>`**. Colors match the user's example bank-form PDF (extracted from its color operators): header band + labels `#2b4c7e`, label cells `#f4f7fa`, borders `#e0e0e0`, amount accent `#b13d3d`, secondary `#64748b`, warm page `#fdfbf7`. Values are HTML-escaped (`_pvdcEsc`).
- **Visual e-signature** (NOT cryptographic PKI) — a dashed **חתימה אלקטרונית / ELECTRONIC SIGNATURE** box containing ONLY the label + the optional **hand-drawn signature** (`signature_pad` canvas → PNG dataURL). (Per user: the name/ID/timestamp/CONFIRMED lines that matched the example were removed — the document body already carries the date.) Live preview updates on every field/stroke change.
- **Export row** — Format radio **PDF** / **Google Doc (DOCX)**, plus **💾 Save** + **🖨 Print / Save as PDF** (the ⬇ Download button was removed per user). **🖨 Print** → native print to disk (perfect Hebrew/colors). **💾 Save** → stores a **DOCX** into a chosen Privacy Site (🔒 Encrypt checkbox; encrypted path reuses privacy.js `pvEnsureUnlocked`/`_pvEncBytes`/`_pvEncStr`/`_pvKey` and POSTs ciphertext to `/api/privacy/sites/:id/docs`, same contract as a Sites-tab upload). Saving with Format=PDF shows a message to use Print instead.
- `js/privacy.js` `showTab()` calls `pvdcOnShow()` on the Doc Create tab (renders the form, inits the sig pad, fills the site dropdown from the global `_pvSites`). privacy.js must load BEFORE privacy-doccreate.js (shared global lexical scope; both are classic scripts).

**Tab: Places** (since 2026-06-16) — a **flat 2D Leaflet/OSM world map** ("travel globe", flat per user pick) of everywhere you've visited, backed by the `visited_places` table (LXC 102). Left card = **📍 Add place** (name / city / country / date / **times visited** / **kind = free text** / **visited-by checkboxes** / notes). **Extra fields (since 2026-06-17):** `visitors` JSONB array of registered-user names (Settings → Users checkboxes — tick one or both), `visit_count` INT (times visited), and **kind is now free text** (was a fixed dropdown). All three show on the list (meta line `kind · 👤 names · ×N`) + the map popup, and are editable in the edit modal. Backend `routes-places.js` handles `visitors` (`::jsonb`) + `visit_count` in POST/PATCH; columns added via `ALTER` (see `scripts/travel_visited_places.sql`). **Locating is interactive (2026-06-16)** because Nominatim isn't fuzzy ("Heraklio Town krete" → no match; "Heraklion, Greece" → hit; bare "Heraklion" → wrong one in Egypt): **🔍 Find** geocodes via **Nominatim** (`nominatim.openstreetmap.org/search`, client-side, `limit=5`, falls back to "city, country") and lists the candidates as clickable buttons — the top match auto-drops an **orange pending pin**, pick another if wrong. **OR just click anywhere on the map** to drop the pending pin manually (handles spellings Nominatim can't resolve). **➕ Add** saves the pending pin's lat/lon + form fields (POST `/api/places`); if no pin is set it does a one-shot geocode as a convenience. State: `_pvPending{Marker,LatLng,Label}` + `_pvpFindResults`. Right = the **map** (circle markers colored by `source`: manual=blue, google_review=green, google_reservation=purple, booking=orange) + a **list** ordered **newest-first** (`visited_at DESC NULLS LAST`) with per-row **Edit** (modal `#pvp-edit-modal` → `PATCH /api/places/:id`, edits name/city/country/date/kind/notes; location is move-by-delete-&-re-add) + Del. Marker popups show name/place/date/notes/source + a 🗑 Delete. CRUD in **`routes-places.js`** (own module past the architecture hook): `GET/POST/PATCH/DELETE /api/places`; manual rows are `source='manual'`, `source_id=NULL` (NULLs distinct in `UNIQUE(source, source_id)` → no collisions). Leaflet loaded via the unpkg CDN in `privacy.html` head (same as network.html/project-general.html); JS in `js/privacy-places.js` (`pvPlaces*`), inited on `showTab('places')` (`_pvMap` guard + `invalidateSize()` after the tab shows). **Context:** built because Google Takeout's Location History was OFF (only 2 points), so the user enters trips manually from Booking.com instead of waiting for the DSAR export. See root `CLAUDE.md` `visited_places` table.

**Tab: Daily Journal** (2026-07-04; **categories added 2026-07-08**) — a personal daily journal, positioned **right of Places**. **N reminders/day** (you set how many, the times, and each name in **Settings → 📓 Daily Journal reminders**) pop a **wide RTL-Hebrew capture panel to the LEFT of the red reminders badge**: `reminders-badge.js` wraps `[journal panel][red badge]` in a fixed top-right flex row (water/exercise stay in the red badge). **Categories ("gloomys", 2026-07-08):** each capture is DIVIDED into user-named categories (e.g. Politics · Weather · Special things) — the panel per due slot = header `📓 <name> · <date>` + **one `dir="rtl"` textarea per active category** (labeled) + **one 😞→😄 (1–5) mood row for the whole reminder** + **💾 שמור / דלג / אחר כך** (Save/Skip/Later). **Mood is ONE per reminder capture** — dropped by migration `008` then re-added by `009` the same day per user request ("only one per reminder"): the client stamps the same mood on every category row of the capture and reads it back from any of them (a mood-only capture, no text, attaches the mood to the first category). Typed text survives the 30 s poll via a category-aware signature guard. Save writes a **row per non-empty category**; the slot's nudge clears once **≥1 category** row exists. The tab shows **Entry** (editable, one box per category + one mood per slot) — with a **date picker to back-date (added 2026-07-15)**: `#pvj-date` defaults to **Jerusalem-today** (`pvjTodayJeru()` = `toLocaleDateString('en-CA',{timeZone:'Asia/Jerusalem'})` so a device abroad still matches the server's day), `max`=today; picking a past day loads THAT day's rows (`GET /api/journal?from=D&to=D`, replacing the old `/today` fetch) to edit, and Save sends `entry_date: D` **only when back-dating** (for today it omits `entry_date` so the server stays authoritative on the date = zero regression / no midnight clock-skew; the `POST` already upserts on `(user_id,entry_date,slot_id,category_id)` — no past-date validation), so a past trip can be logged (e.g. type the whole trip into one box on its first day = one record). An amber hint shows while back-dating. Header relabeled `📓 Today`→`📓 Entry`. A **🔍 Search** card (**category dropdown + keyword + date-from/to** → `GET /api/journal/search`, full history), and a **History timeline** (Hebrew RTL, grouped **Day → reminder**; the mood emoji shows **once per reminder** above its category lines — never per-category; each category line = chip + text). The old **mood-trend Chart.js was removed**. **Storage: `journal_entries`** (LXC 102 — `id, user_id FK household_users [Vadim=1], entry_date DATE, slot_id TEXT, slot_name TEXT snapshot, category_id TEXT NOT NULL DEFAULT 'general', category_name TEXT snapshot, comment TEXT (UTF-8 Hebrew), mood SMALLINT 1–5 nullable, created_at, updated_at`; **UNIQUE `(user_id, entry_date, slot_id, category_id)`** → upsert per slot+category; **retention forever + 🔒 protected**; migrations `007_journal.sql` + `008_journal_categories.sql` + `009_journal_mood.sql`). **Plaintext (LAN-only) on purpose** — **AI-readable** for a future reflection, UNLIKE the site 📄 Docs (client-side-encrypted/server-blind). Config in `dashboard_settings.journal = {enabled, user_id, slots:[{id, name, time_hm}], categories:[{id, name}]}` (slot AND category `id` are stable so rename/reorder/remove doesn't break history; entries snapshot both `slot_name` + `category_name`; if `categories` is empty a single `General` is used). Backend **`routes-journal.js`** (own module past the architecture hook): `GET /api/journal[?from&to]` · `GET /api/journal/today` · **`GET /api/journal/search?cat=&q=&from=&to=`** (category = `category_id`, keyword = `comment ILIKE`, date range; all optional; LIMIT 500) · `POST /api/journal` (upsert with `category_id`/`category_name`; both default to `general`) · `DELETE/:id` (no PATCH — edits are POST-upserts keyed by user+date+slot+category). **Reminder source** in `routes-reminders.js`: for each config slot, once `time_hm` has passed today (Asia/Jerusalem) AND no `journal_entries` row for (user, today, slot_id) → a `kind:'journal'` item (`rkey journal:<uid>:<date>:<slot_id>`) carrying `slot_id`/`slot_name`/`entry_date`/**`categories`**; **Save** writes the rows → the nudge disappears; **Skip** = Clear; **Later** = snooze. The done-check is category-agnostic (`count(*) WHERE slot_id` = any row). The panel Save dispatches `ph-journal-changed` so the tab's Today/history refresh live. Front-end: `js/privacy.js` `pvJournal*` (`pvJournalOnShow`, `pvJournalCfg*`/`pvJournalCat*` editors, `pvJournalRenderToday/Timeline`, `pvJournalSearch*`, `_pvjEntryList`), `PVJ_UID=1`, moods `PVJ_MOODS = ['😞','😕','😐','🙂','😄']`. **Phase 2 (deferred):** a `POST /api/journal/reflect` (reuses the existing `ANTHROPIC_API_KEY` the boiler AI uses) → weekly reflection narrative — **opt-in**, journal text leaves the LAN only when pressed. Cache-busts `privacy.js?v=84` + `reminders-badge.js?v=6` (bumped on 19–20 pages).

**Settings tab — Users card** (since 2026-06-17) — a household-members registry: editable rows of **real name + smartphone** (free text, e.g. "Galaxy Fold5"), modeled on the Gateway peer→user overlay. Rows auto-save on `change`; `+ Add user` appends a blank row; ✕ deletes. The **smartphone field is an autocomplete** (`<datalist id="pv-phones-datalist">`, type-or-pick) fed by **two sources** (since 2026-06-17): (1) geolocation **tracked phones** (`GET /api/geolocation/settings` → `tracked_devices[].name`, e.g. "Vadim Fold 5") + (2) **named network devices** (`GET /api/network/devices`) **filtered to phones** — kept if name has a phone-model token (`galaxy|iphone|pixel|fold|flip|note N|[sa]NN|phone|…`) OR a randomized **"Locally administered"** MAC, MINUS an explicit non-phone exclude list (`tv|soundbar|robot|tablet|asus|switch|sensor|light|…`) so the 111 home-automation rows + Samsung TVs/soundbar don't pollute it (yields just the real phones, e.g. "Maya S 21", "Samsung Fold 5"). Still free-text-capable. **Backed by the canonical `household_users` TABLE (LXC 102) since 2026-06-25** via `/api/household-users` (`routes-household-users.js`) — this REPLACED the old `dashboard_settings.privacy.users` JSON blob, which was deleted. Each row has a stable `id`: a new row POSTs on its first named edit, field edits PATCH, ✕ DELETEs **with confirm** (cascades the member's Personal-Health data via `ph_profiles.user_id` FK). A **phone-number** field (☎) was added alongside the device label (📱 — the old `smartphone` → `device_label`); the table also carries `ntfy_topic`/`role`/`active`/`notes` for future use. JS in `js/privacy.js` (`pvLoadUsers`/`pvLoadPhoneOptions`/`pvRenderUsers`/`pvUserSet`/`pvUserDelete`/`pvAddUser`), loaded on `showTab('settings')`. Schema/migration: `PRIVACY/004_household_users.sql`. The FR/media/gateway/places person fields stay name-based (broader populations) with `household_users.name` as the canonical member-name source. See root `CLAUDE.md` → `household_users`.

**Settings tab — Reminders card** (since 2026-06-26) — config for the **project-wide reminders badge** (a red square pinned top-right that lists what's **due** per person). Lives in Privacy → Settings (not Project Health — these are personal, per-user reminders, and this is where the `household_users` they belong to are managed). Controls persisted in `dashboard_settings.reminders = {enabled, snooze_min, med_window_hours, pages[]}`: **Enable** toggle, **Delay (snooze) minutes** (default 30), **💊 Late-dose carry-over (hours)** (default 8, 0–24; added 2026-06-27), and a **page checklist** (which of the 19 dashboard pages show the badge — `index`/`main-agent`/…/`medical`). JS: `pvLoadReminders`/`pvSaveReminders` + `_PV_REM_PAGES` in `js/privacy.js`, loaded on `showTab('settings')`. **Backend evaluator = `BOILER/dashboard/routes-reminders.js`** (own module past the architecture hook): `GET /api/reminders` returns `{enabled, snooze_min, pages, items:[{rkey,user_name,label,kind}]}` by evaluating **sources** — (1) **medications** (active `ph_medications` whose schedule is due today, and — when `times` are set — only once that HH:MM has passed in Asia/Jerusalem; **late-dose overnight carry-over (2026-06-27):** a timed dose also stays visible up to `med_window_hours` after its time *past midnight*, keyed to the dose's own date so a pre-midnight Clear persists — daytime doses still show until end of day, `med_window_hours=0` reverts to clear-at-midnight) + (2) **measure schedules** (`ph_profiles.weight_sched`/`bp_sched`/`body_sched` overdue: nothing logged in `ph_measurements`/`ph_bp`/`ph_body` within the window — the **📏 Waist & hip** source added 2026-06-27) + (3) **water interval nudge** (2026-06-27 — GLOBAL config `dashboard_settings.medical.water` `{enabled,target,start_hm,end_hm,interval_min}`, edited on Medical → Settings → 💧 Water: `interval = interval_min || window÷target`; for every profile, when `ph_water` cups-so-far-today < `min(target, floor((now−start)÷interval))`, badge shows **💧 Drink water (N/target)**; `rkey water:<pid>:<date>:<slot>`, clears as cups are logged, silent outside the window. **Tapping Clear on a water reminder LOGS +1 cup** (2026-06-27 — Clear means "I drank it"; `POST /api/reminders/clear` inserts a `ph_water` row for the profile parsed from the rkey, then dismisses — so the count keeps up with the pace; Clear on med/weight/bp is dismiss-only as before). ntfy push later). New sources are pluggable functions — no UI/engine change. `POST /api/reminders/snooze {rkey}` snoozes until `now + snooze_min`; `POST /api/reminders/clear {rkey}` clears that occurrence. Per-instance state in **`reminder_state`** (LXC 102, migration `PRIVACY/migrations/005_reminders.sql`, retention 30 d auto-clean): `rkey PK, snoozed_until, cleared_at, updated_at`; an `rkey` is suppressed when `cleared_at` is set OR `snoozed_until > now()`. **`rkey` is occurrence-stamped** (`med:<id>:<date>:<slot>`, `weight:<profile>:<window>`, `bp:<profile>:<window>`) so a Clear/Delay sticks for that dose/window and the next occurrence raises a fresh reminder. **Frontend badge = shared `js/reminders-badge.js`** (an IIFE loaded on all 19 pages next to `alerts-monitor.js`; viewer.html excluded): self-injects a fixed top-right red card, polls `/api/reminders` every 30 s, and renders **only if `enabled` AND the current page slug (from `location.pathname`) is in `pages`** — so the page list is runtime-configurable with no HTML edits. Each row shows `<User> — <💊 pill / ⚖ Weight / 🩺 BP>` with **Clear** + **Delay** buttons. **Future:** a server-side notifier (LXC-104, alongside the planned pill/ntfy work) can reuse the same evaluator/`reminder_state` to push beyond the in-page badge.

**Tab: Budget** (x-spreadsheet, since 2026-06-29) — an **Excel-like multi-sheet spreadsheet**. Renamed from the earlier hand-built "**Offers**" prototype tab (the prototype grid was flushed); the **internal slug/IDs stay `offers`/`tab-offers`/`pvof*`** and the storage key stays `dashboard_settings.privacy.offers` (only the visible label is "Budget"). Built on the **x-spreadsheet** library (MIT, **vendored locally** in `public/vendor/sheets/` = `xspreadsheet.css` + `xspreadsheet.js` + the icon sprite **`58eaeb4e52248a5c75936c6f4c33a370.svg`** — ALL 3 are required; the CSS pulls every toolbar icon from that sprite, so a missing sprite = a **blank toolbar**, the one gotcha hit during build). Global factory = `window.x_spreadsheet(el, opts)`. **Has:** formulas (SUM/AVERAGE/MIN/MAX/COUNT + cell refs, auto-evaluated), cell fill + text colour, merge/split, number/text formats, freeze, copy/paste, undo/redo, insert/delete rows+cols, **multiple sheets** (bottom tabs). **Lacks** (accepted): conditional auto-colour + charts — **charts will come from our own dashboard Chart.js fed by the sheet data**, not the library. **PHASE 1 (done, was plaintext):** `js/privacy-offers.js` inited the grid lazily on tab-show and saved the native workbook JSON (**array of sheet objects**) to `dashboard_settings.privacy.offers`. **PHASE 2 (DONE 2026-06-29, server-blind encryption):** storage moved to a dedicated **`privacy_sheets` table on LXC 102** (singleton `id=1`, `enc_data`/`enc_iv`/`updated_at`, **protected/forever**, migration `006_privacy_sheets.sql`). Endpoints **`GET/POST /api/privacy/sheet`** in `routes-privacy.js` only ever store/return **ciphertext** (server blind). All crypto **reuses the SAME Documents password + helpers in `privacy.js`** (`_pvKey`/`_pvEncStr`/`_pvDecStr`/`pvEnsureUnlocked`; AES-256-GCM, PBKDF2-600k, salt+verifier in `privacy_doc_crypto`) — so unlocking Documents also unlocks Budget in the same page session, and vice-versa. **Whole-workbook lock model:** the Budget tab is **🔒 Locked by default** (a blank "Unlock to view" overlay over the grid — no cells/layout/values shown); **🔓 Unlock** → `pvEnsureUnlocked()` (prompts for the Documents password, sets `_pvKey`) → `GET /sheet` → decrypt → `loadData`. **💾 Save** → `getData()` → `_pvEncStr` → `POST /sheet` (ciphertext). **One-time auto-migration:** on the first unlock, if `privacy_sheets` is empty but `privacy.offers` still holds plaintext, the client loads it, encrypts + saves it, then **wipes the plaintext key** (`POST privacy.offers null`) — verified live (15 KB ciphertext, no `name`/`cells`/Hebrew leak, plaintext gone). **👁 Show-password toggle** on the password modal (`pv-pw-show`, inline; resets to hidden on every modal open, `privacy.js`). **Rows/Cols box** (`pvof-rows`/`pvof-cols` + `pvofApplySize`, shown only when unlocked) — resize the grid (1–5000 rows / 1–100 cols, all sheets), inputs auto-fill the current size, persists in the workbook JSON on Save. **Newline in a cell = Alt+Enter** (+ text-wrap to display). **⚠ KNOWN-DEFERRED:** the Budget **🔒 Lock** button currently **hides-only** (blanks the grid but keeps `_pvKey` in memory), so a subsequent Unlock re-opens **without** re-asking the password; making Lock *truly* re-lock (clear `_pvKey`) is a deliberate later task. **No recovery:** a forgotten Documents password = the encrypted workbook is unrecoverable unless the page is still unlocked in memory (the plaintext was wiped at migration) — same server-blind guarantee as Documents. **PHASE 3 (DONE 2026-06-29, backups — home + off-site, both ciphertext):** **(A) Home copy reuses the existing Windows backup** (Project Health → LXC 104 `backup-script.sh`): on every Budget **Save**, `routes-privacy.js` also writes the encrypted blob `{enc_data,enc_iv,updated_at}` to **`C:\Users\muroc\PrivacyBackup\privacy_budget.json`** (ciphertext mirror, `BUDGET_MIRROR_DIR` const, mkdir on module load, best-effort write — never fails the save). A `backup_jobs` row **"Privacy Budget"** (source `muroc@192.168.1.128:C:/Users/muroc/PrivacyBackup` → storage Claude_Data/QNAP, dest_subdir `Privacy_Budget`, `max_age_hours=24` daily, retention 7) makes the existing agent scp it to QNAP nightly with the usual logging/retention. *(`max_age_hours` is the backup CADENCE, not a source-freshness check — an unchanged Budget never alerts.)* Verified: backup ran `ok`, file landed at `/mnt/qnap-claude/Privacy_Budget/<ts>/PrivacyBackup/privacy_budget.json`. **(B) Off-site copy = Google Drive** via a **dedicated rclone remote `gdrive_sheets` scoped `drive.file`** on LXC 104 — that scope lets rclone touch **ONLY its own files**, so it can NEVER read/modify/delete the rest of the user's Drive (proven live: `rclone lsd gdrive_sheets:` shows only `Privacy_Budget`, while the old read-only `gdrive:` lists the whole Drive). Script **`scripts/privacy-budget-cloud-backup.sh`** → `/opt/` (dumps the blob from LXC 102 → `rclone copyto` a dated copy + `privacy_budget_latest.json` to `gdrive_sheets:Privacy_Budget/`, prunes dated > 30 d), cron **`20 3 * * *`** (just after the `15 3` vault backup). One-time setup was `rclone authorize "drive" --drive-scope=drive.file` on the laptop (browser consent) → token into the LXC-104 remote. **3 copies, all ciphertext:** DB (live) + QNAP (home) + Drive (off-site) — same protection tier as the Cryptomator vault. Cache-busts `privacy.js?v=73` + `privacy-offers.js?v=9`. Health DB-Volumes/Retention registered (`server.js` `DBV_GROUPS` Privacy + `tsCol`). Full build plan: [SPREADSHEETS_PLAN.md](SPREADSHEETS_PLAN.md).

---

**Original plan below (design rationale + remaining phases).**

## Goal
A dedicated, private LXC to: (1) **store documents that can only be opened with a password**, and (2) **self-host a password manager**. Redundancy to **Google Drive** (offsite) + **QNAP** (local). **LAN-only** (nothing exposed to the internet).

## Locked design
| Part | Solution |
|---|---|
| **New LXC** | "PRIVACY" (next free ID ~109), Debian 12, LAN-only, static `192.168.1.x` |
| **Passwords** | **Vaultwarden** on the LXC (self-hosted Bitwarden-compatible, Rust, ~100 MB RAM). E2E / zero-knowledge — vault encrypted with the master password; the server never sees plaintext. Remote = **Bitwarden app offline cache** (chosen over NetBird) |
| **Docs** | **Cryptomator** encrypted vault → **Google Drive** (remote view) + copy on LXC + backup on QNAP. All ciphertext; **per-session unlock** with the master password — home AND away, no plaintext shortcut |
| **Backups** | **QNAP** (existing LXC 104 cron infra) + **Google Drive** (`rclone`), both encrypted |
| **Cloud** | Google **Drive free tier** (15 GB) on the user's gmail |
| **Access** | LAN-only at home; remote docs via Cryptomator+Drive; remote passwords via the offline cache |

## Security model (the load-bearing part)
- An LXC's filesystem is **readable from the Proxmox host (root)**. So "only by password" is real ONLY because the data is **encrypted with a key derived from the password, not stored on disk**:
  - **Vaultwarden** — inherently E2E (master password) → satisfied automatically.
  - **Docs** — Cryptomator **client-side** encryption → ciphertext at rest *everywhere* (LXC / QNAP / Drive); only the user's device decrypts, with the password.
- **Anything sent to Google MUST be ciphertext** (Google can read plaintext). The Cryptomator vault is already ciphertext → safe to sync as-is; the Vaultwarden backup blob is wrapped with `rclone crypt`/`age` before Drive.
- **Per-session unlock:** Cryptomator asks for the password once per session (not per file); locks on close/idle. This *is* the privacy guarantee — the only way to skip it is plaintext (rejected).
- **Nothing exposed to the internet.** LAN-only; remote docs via Drive+Cryptomator, remote passwords via the Bitwarden offline cache.

## Password-manager capabilities (Vaultwarden server + Bitwarden clients)
- Store all passwords + secure notes + **TOTP/2FA** + attachments + generator.
- **Autofill:** browser extension fills matched logins by URL (websites); mobile OS autofill fills *other apps* on the phone. **Native Windows desktop apps = copy-paste** (limited auto-inject — the one spot it's not fully automatic).
- **Suggest + generate a strong password on signup**, then auto-saves the login (like Chrome/Edge's built-in suggestion).
- **Offline cache:** the Bitwarden apps keep an encrypted local copy → read/autofill when away; edits sync when back on the LAN.

## Security review + hardening (2026-06-12)
**Verdict: keep Vaultwarden + Cryptomator with the hardening below — do NOT switch to the no-server KeePassXC variant** (for this home, non-targeted user). Reasoning: KeePassXC's only security win is removing the self-hosted server surface, which is already contained here (LAN-only, HTTPS, native clients not the web vault, patched). Bitwarden's far better usability (autofill, generator, live sync) produces *better real-world* hygiene (unique strong passwords everywhere) than KeePassXC's friction. The dominant risks are identical in both designs (see below) and the user has the ops discipline to keep one more LAN-only service patched. **Flip to KeePassXC only if:** targeted high-value subject, or "zero self-hosted services" on principle.

**The risks that actually dominate (independent of tool choice) + mandatory hardening:**
1. **Master-password strength is the root of trust, and the encrypted blobs LEAVE the house (Drive + QNAP) → offline brute-force is the real threat.** → long, high-entropy, *unique* passphrases for Vaultwarden AND Cryptomator; **set Bitwarden KDF to Argon2id**. Non-negotiable.
2. **Endpoints are the weak link** (plaintext in RAM when unlocked) → laptop/phone patched, **full-disk-encrypted (BitLocker)**, auto-lock.
3. **Google account holds the offsite ciphertext** → strong **2FA (hardware key/TOTP, not SMS)** on the gmail.
4. **Forgotten master password = permanent loss** → secure **recovery copy** (printed, in a physical safe) + Bitwarden Emergency Access.
5. **Vaultwarden server hygiene** → HTTPS even on LAN; use native app + browser extension (avoid the web vault, which a compromised server could trojan to steal the master password); lock down `/admin`; patch discipline; Vaultwarden 2FA for online login.

Component notes: Vaultwarden vault = client-side E2E (server never holds the key); Cryptomator = client-side AES-256, Cure53-audited. Metadata caveats: Vaultwarden DB leaks account email/timestamps/counts (contents encrypted); Cryptomator leaks file sizes/count/mtimes (names + content encrypted). Both acceptable for this use. **Pre-build TODO:** verify current Vaultwarden/Cryptomator CVEs + audit status before deploying (knowledge has a cutoff; do a quick web check at build time).

## CVE / security web-check (done 2026-06-13)
Web-checked current CVEs/audit status before committing to the build. **Verdict: design unchanged — Vaultwarden + Cryptomator remains sound for a single-user, LAN-only, patched setup.** Findings:

- **Vaultwarden — 3 recent CVEs, all about MULTI-USER ORGS / the admin panel, all patched. None break single-user vault crypto.**
  - `CVE-2026-43912` cross-organization data access → fixed in **1.35.5**
  - `CVE-2026-27802` privilege escalation (bulk permission update) → fixed in **1.35.4**
  - `CVE-2026-26012` org cipher auth bypass → fixed in **1.35.3**
  - earlier 2025 advisories: CSRF + **RCE in the admin panel** + priv-esc.
  - **→ Firm build requirements (added by this check):** deploy **Vaultwarden ≥ 1.35.5**, run **single-user (no organizations)**, **lock down `/admin`** (strong `ADMIN_TOKEN` or disable the admin panel), keep patched. The E2E vault is not affected by any of these.
- **Cryptomator — clean.** Cure53-audited (crypto "exceptional robustness", no break). Only minor advisories: a low-severity Mar-2026 issue (two ciphertext files could be swapped) + a 2025 MSI-installer local priv-esc. No serious CVE. Verdict stays: solid.
- **Argon2id KDF** — Bitwarden supports it (OWASP-recommended). At build time set concrete params (OWASP baseline ≥ 19 MiB / 2 iters / 1 parallelism, raised higher since our encrypted blobs go offsite to Drive/QNAP → offline brute-force is the real threat); bump memory in ~100 MB steps and test on all devices.

Sources: [Vaultwarden advisories](https://github.com/dani-garcia/vaultwarden/security/advisories), [Cryptomator security](https://github.com/cryptomator/cryptomator/security), [Cryptomator audit](https://community.cryptomator.org/t/has-there-been-a-security-review-audit-of-cryptomator/44), [Bitwarden KDF](https://bitwarden.com/help/kdf-algorithms/). Re-check at actual build time if much later than 2026-06-13.

## Build plan (phases — pending approval, one at a time)
1. **Provision the LXC** — Proxmox host `pct create`; Debian 12; 2 vCPU / 2 GB RAM / 16 GB disk (more if many docs); static IP.
2. **Vaultwarden** — Docker compose (`vaultwarden/server`), persistent data volume, **signups disabled after account created**, HTTPS via Caddy / internal cert.
3. **Cryptomator doc vault** — recommended topology: the PC creates the vault **inside the Google Drive folder** (Drive syncs it everywhere = remote view + offsite copy); the **LXC pulls a copy** (so the LXC "keeps the docs"); **QNAP backs it up**. (Alt: LXC-primary, shared to the PC over the LAN — clunkier.)
4. **`rclone` → Google Drive** — install on the LXC, one-time gmail OAuth, dedicated Drive folder; `rclone crypt` layer for the Vaultwarden backup blob.
5. **Backups (nightly)** — Vaultwarden data → encrypted archive → QNAP + Drive; Cryptomator vault (already ciphertext) → QNAP + Drive. Scheduled via the existing **LXC 104** cron pattern.
6. **Clients** — Bitwarden Windows app + browser extension + phone (Server URL = the LXC; offline cache on by default); Cryptomator on laptop + phone; Google Drive desktop client on the PC.
7. **Integration** — this `CLAUDE.md`, an infra-table row in root `CLAUDE.md`, retention/backup registration, and (optional) a **Project Privacy dashboard tile** (Vaultwarden up? last backup? Drive sync OK?).

## Decisions already made
- **Password manager = Vaultwarden** (over KeePassXC / Passbolt / Nextcloud-Passwords) — lightweight, E2E, full Bitwarden client ecosystem.
- **Docs encryption = Cryptomator** (over gocryptfs) — chosen for first-class mobile apps so the Drive copy is viewable on a phone/laptop with the password.
- **Remote passwords = offline cache** (option A, over NetBird) — simplest, nothing exposed.
- **Cloud = Google Drive free tier.**
- **Access = LAN-only.**
- **Per-session password unlock** for docs is accepted (the privacy trade-off).

## Open decisions / needed to start
1. **Proxmox host access** to create the LXC — or the user creates an empty Debian 12 LXC + gives SSH (can't `pct` from inside an LXC).
2. **gmail** for the rclone OAuth (user clicks through the one-time auth).
3. **Doc topology** — PC+Drive-primary (recommended) vs LXC-primary.
4. **Final disk size** — depends on doc volume (15 GB is the free-tier Drive cap).
5. **Optional dashboard tile** — build a Project Privacy status tile, or keep standalone.

## Convention note
Folder is `PRIVACY/` (correct spelling, matching the other module folders). The user wrote "PRIVECY" in chat — treated as a typo; confirm if the literal spelling is wanted.
